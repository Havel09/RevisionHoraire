# RevisionHoraire
## Fischer Adrian

## Setup

Pour start la base de données via docker voici la commande à faire :

```
docker compose up -d
```

Puis ensuite pour lancer le site web : 

```
php -S localhost:8000
```